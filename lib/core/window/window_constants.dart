/// Centralized constants for window dimensions.
class WindowConstants {
  /// The collapsed toolbar height (no plugin open).
  static const double kToolbarWindowHeight = 56;

  /// The default window width.
  static const double kDefaultWindowWidth = 450;

  /// The expanded window height (plugin panel open).
  static const double kExpandedWindowHeight = 500;

  /// The square mode dimension (nxn).
  static const double kSquareModeSize = 850;
}
